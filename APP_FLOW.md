# 心语 V2 应用流程文档

## 0. 文档目的与阅读规则

本文件用纯中文定义心语 V2 的页面、触发条件、用户动作、导航路径、成功状态和错误状态。页面编号只用于研发和验收定位，不代表用户界面必须显示编号。

本文件适用于：

- 学生端微信小程序；
- 独立桌面 Web 管理后台。

学生端和后台是两套入口。后台不出现在小程序底部导航中，也不被压缩成移动端页面。

如果历史原型、旧文档或视觉预览与本文件不同，以本文件以及最新一轮 V2 规范为准。视觉预览中的静态装饰按钮不自动产生新的导航或业务功能。

## 1. 全局导航规则

### 1.1 学生端主导航

学生端完成首次使用后，底部固定显示四项：今日、自测、树洞、我的。点击任意一项都进入对应模块的首页，并保留当前账户会话。

- 今日：查看每日短句、今日心情和快捷入口。
- 自测：选择量表或睡眠观察并查看最近记录。
- 树洞：浏览匿名内容、进入发布和详情。
- 我的：查看历史、支持资源、我的内容、匿名身份和数据隐私。

答题、结果、安全支持、发布确认和删除确认等关键页面可以暂时隐藏底部导航。用户完成或退出关键动作后，回到原来的模块首页。

### 1.2 返回规则

- 有明确上一级页面时，左上角返回回到上一级，不重置已保存数据。
- 从根页面返回时，回到微信上一层页面或停留在当前根页面，不退出账户。
- 答题中返回必须提示“当前回答尚未完成”；确认离开后保存为未完成会话，不生成结果。
- 安全支持页面返回只回到安全确认或结果流程，不通过返回键绕过安全分支。
- 发布检查中返回不能把检查中的帖子伪装成已公开。

### 1.3 会话失效规则

任何页面收到会话失效响应时：

1. 停止继续请求；
2. 清除本地会话令牌和页面中的敏感详情；
3. 显示“登录状态已过期，请重新进入”；
4. 用户确认后进入登录恢复流程；
5. 未提交的普通表单可以保留在当前页面，但姓名、学号、完整答案和安全确认不写入持久化本地存储。

### 1.4 网络和请求规则

- 请求中按钮进入不可重复点击状态。
- 保存和提交请求必须带幂等标识。
- 网络失败只显示失败，不显示成功假状态。
- 可重试的请求显示“重新加载”或“再次保存”。
- 版本冲突必须显示服务器当前事实，页面转为只读，不能用旧页面覆盖新状态。

## 2. 首次使用与登录流程

### 2.1 首次打开流程

触发条件：用户第一次打开小程序，或本地没有有效的服务端会话。

导航路径：打开小程序 → 首次使用页 → 基础服务同意 → 身份核验说明 → 输入核验信息 → 核验成功 → 今日页。

步骤：

1. 系统显示首次使用页，说明心语是自我观察和匿名轻社区，不是医疗诊断或危机处置工具。
2. 用户阅读基础服务用途、保存范围和停止使用方式。
3. 用户点击“同意并继续”。
4. 小程序调用微信登录，服务端建立学生会话。
5. 服务端记录同意版本、时间和来源。
6. 用户进入身份核验说明页，完成姓名和学号核验。
7. 核验成功后生成默认匿名身份，用户进入今日页。

成功状态：身份核验完成并生成默认匿名身份，今日页显示短句、今日心情入口和自测入口；用户可以继续浏览。

暂不继续状态：用户点击“暂不使用”时回到微信上一层或停留在说明页，不创建可用的业务会话，不伪造同意成功。

错误状态：

- 微信登录失败：显示“暂时无法进入，请稍后重试”，保留重新进入按钮；
- 服务端不可用：显示“服务暂时不可用”，不显示已同意或已登录；
- 同意记录失败：不进入需要账户的页面，允许重新提交；
- 身份核验失败或不可用：停留在身份核验页，不进入今日，不伪造匿名身份；允许检查信息或稍后重试；
- 会话已经停止使用：显示恢复入口，不允许创建新的观察或社区内容。

### 2.2 进入身份核验流程

触发条件：首次使用进入今日前，或后续保存需要账户绑定的私密观察、查看历史、发布树洞或管理数据而身份尚未核验。

导航路径：首次使用 → 身份核验说明 → 输入核验信息 → 核验结果 → 今日；后续功能 → 身份核验说明 → 输入核验信息 → 核验结果 → 返回原功能。

步骤：

1. 页面说明姓名和学号只用于学校身份核验，不会显示在树洞中。
2. 用户填写姓名和学号，点击“开始核验”。
3. 服务端调用已配置的学校核验适配器或进入明确的人工核验状态。
4. 核验成功后，服务端只保存受保护的身份记录和不可逆内部引用。
5. 用户返回触发核验的页面。

成功状态：显示“身份已核验”，不显示真实身份给其他用户；原功能可以继续。

错误状态：

- 信息不匹配：显示“信息未通过核验，请检查后重试”，不透露数据库中的正确答案；
- 核验服务不可用：显示“核验服务暂时不可用”，不显示成功；
- 未配置核验服务：显示“当前环境尚未配置身份核验”，进入演示或联系管理员提示；
- 用户取消：回到原功能，原功能保持未提交状态；
- 会话失效：清除姓名、学号输入，要求重新登录。

## 3. 今日模块流程

### 3.1 今日页

页面编号：今日页。

触发条件：首次使用完成、点击底部“今日”或从其他模块返回。

页面内容顺序：

1. 一条每日短句和出处；
2. 今日心情卡片；
3. 自测快捷入口；
4. 支持资源入口；
5. 非强制的最近观察提示。

导航路径：今日 → 心情选择；今日 → 自测中心；今日 → 支持资源；今日 → 我的。

短句规则：每天最多显示一条启用短句，格式为“短句——作者 / 作品”。短句来自项目维护的静态库或版本化配置库，不运行时联网搜索、不由模型生成。

成功状态：页面正常显示今日内容；已保存的心情显示“今天已记录”和保存时间。

空状态：短句库暂时没有可用内容时显示温和的默认说明，不调用网络临时生成；心情尚未保存时显示选择入口。

错误状态：

- 今日接口失败：短句和本地静态内容仍可显示，心情区域显示重新加载；
- 配置缺失：显示“内容暂未配置”，不能虚构短句作者或支持资源；
- 会话失效：页面保留非敏感说明，进入登录恢复流程。

### 3.2 今日心情选择

触发条件：用户点击今日页的心情入口。

步骤：

1. 页面显示固定的心情选项和“暂不记录”说明。
2. 用户选择一个心情。
3. 用户点击“保存今天的心情”。
4. 服务端按用户和日期幂等保存；同一天已有记录时返回已记录事实，不覆盖当天已经保存的选择。
5. 页面返回今日页并显示已保存状态。

成功状态：按钮变为已保存，显示保存时间；当天再次进入只显示已记录事实，不提供修改入口。

错误状态：

- 未选择：按钮不可提交并提示先选择；
- 网络失败：保留当前页面选择，显示“没有保存成功，再试一次”；
- 会话失效：不保存，进入登录恢复；
- 账户停止使用：显示恢复账户后才能记录，不显示保存成功。

## 4. 自测模块流程

### 4.1 自测中心

页面编号：自测中心。

触发条件：点击底部“自测”或点击今日页的自测入口。

页面显示三个固定模块：抑郁情绪自测、焦虑自测、睡眠观察。页面同时显示用途、预计耗时、最近记录时间和开始按钮。

导航路径：自测中心 → 选择模块 → 答题页 → 结果页；自测中心 → 历史记录。

成功状态：三个模块均可进入，最近记录只显示本人记录。

错误状态：

- 目录加载失败：保留页面标题和重新加载按钮；
- 未完成身份核验：点击开始时进入身份核验，不丢失模块选择；
- 账户停止使用：只显示历史或恢复入口，不允许开始新的会话。

### 4.2 答题页

触发条件：用户在自测中心点击某个模块的开始按钮。

步骤：

1. 页面显示当前问题、固定选项和进度。
2. 用户选择一个答案，进入下一题；如果 PHQ-9 第 9 题选择非零，立即暂停答题并进入安全确认页。
3. 用户可以返回修改已经作答的问题。
4. 最后一题完成后，用户点击“提交本次观察”。
5. 服务端校验题目版本、答案数量、答案取值和幂等标识。
6. 普通模块全部完成后进入结果计算；睡眠观察进入观察摘要计算；PHQ-9 必须先完成安全确认并继续完成剩余题目，才在全部完成后计算结果。

离开流程：用户点击返回或切换页面时出现离开提示。确认离开后会话标记为未完成，答案不进入历史结果；取消则回到答题页。

成功状态：提交按钮只响应一次，页面显示处理中；安全确认已经在第 9 题非零时完成，服务端最终成功后进入结果页。

错误状态：

- 未答完：提示剩余题目并定位到第一道未答题；
- 答案格式错误：显示“请重新选择当前答案”，不保存非法值；
- 网络失败：当前答案保留在内存中，显示重试；
- 重复提交：显示同一会话的处理中或已有结果，不生成重复结果；
- 版本冲突：提示问卷已更新，用户重新开始新会话；
- 会话失效：清除敏感答案并要求重新登录。

### 4.3 普通结果页

触发条件：PHQ-9、GAD-7 或睡眠观察完成，且未进入无法保证安全的分支。

步骤：

1. 页面显示本次模块名称和完成时间。
2. PHQ-9 或 GAD-7 显示服务端按固定规则计算的本次分数和解释；睡眠观察显示三维观察摘要，不显示医学总分。
3. 页面显示非诊断说明和温和的下一步建议。
4. 用户可以进入支持资源、历史记录或返回自测中心。

成功状态：结果快照已经保存，刷新后仍可从历史记录打开。

普通和较高分数结果在固定规则内容之后可以加载可选的 AI 辅助说明；AI 加载失败时显示固定规则说明，不能阻塞结果、历史或支持资源。

错误状态：

- 结果保存失败：显示未保存，允许重新提交同一会话；
- 结果读取失败：不显示不完整分数，显示重新加载；
- 结果快照损坏：显示“本次记录暂时无法展示，请稍后重试”，记录后台错误但不暴露技术细节。

### 4.4 较高分数结果页

触发条件：PHQ-9 或 GAD-7 达到服务端固定的较高分数展示规则，且没有安全支持阻断。

导航路径：答题页 → 结果页 → 支持资源或历史记录。

页面行为：

- 固定结果和支持资源先显示；可选的 AI 辅助说明只在固定结果完成后加载；
- AI 不改变分数、参考分层、支持优先级或页面状态；

- 说明这只是一次自我观察结果，不是诊断；
- 建议用户考虑联系可信任的人或专业支持；
- 提供支持资源按钮；
- 不显示疾病名称和风险等级红黄绿区，不把 AI 辅助说明当作诊断或风险判断；
- 不因“较高分数”自动创建安全任务。

成功状态：结果保存并可在历史记录中查看，内容仍属于本人私密记录；AI 辅助说明失败时仍有固定规则结果。

错误状态与普通结果页一致；如果同时满足安全分支，改走安全支持流程。

### 4.5 睡眠观察结果页

触发条件：睡眠观察八题完成。

页面行为：

- 以入睡、夜间情况、白天感受三个观察维度展示摘要；
- 可以显示趋势或单次观察，但不计算临床总分；
- 使用“最近”“可以留意”等中性词；
- 提供支持资源和再次观察入口。

成功状态：观察快照保存，历史记录可查看。

错误状态：缺少某一维度时不显示伪造完整图形，明确显示数据不足和重新加载入口。

### 4.6 安全确认页

触发条件：用户在 PHQ-9 答题过程中将第 9 题选择为非零选项。

导航路径：答题页 → 安全确认 → 答题页继续完成或支持资源；无法保证安全时不进入完整结果页。

步骤：

1. 页面先说明需要确认当前是否安全，不展示多余分数、历史、身份和模型内容。
2. 用户选择“我现在可以保证安全”“我不太确定”或“我现在无法保证安全”。
3. 系统按固定分支处理，不让模型参与判断。

分支一：我现在可以保证安全。

- 保存安全确认的最小状态；
- 返回答题页继续完成剩余题目；
- 全部题目完成后进入普通或较高分数结果；
- 不创建后台安全任务；
- 支持资源仍然可进入。

分支二：我不太确定。

- 进入支持资源页；
- 用户可以查看资源、返回修改第 9 题回答或主动继续；
- 主动继续后返回答题页完成剩余题目；
- 全部题目完成后保存受限安全状态；
- 演示或明确授权环境可以创建最小安全支持任务；
- 结果页不显示不必要的风险标签。

分支三：我现在无法保证安全。

- 不再回到答题页，立即进入支持资源页；
- 提供联系可信任的人、校内支持和当地紧急支持入口；
- 提供离开和修改回答动作；
- 不继续展示完整结果、历史、模型内容或真实身份；
- 演示或明确授权环境只创建最小安全支持任务。

安全确认成功状态：分支事实被保存，页面显示对应下一步，不能把“已展示资源”写成“用户已安全”。

如果用户返回答题页并把第 9 题改为零，最终提交时服务端以最新答案为准，清除临时安全触发状态，按普通或较高分数结果处理，不创建安全支持任务。

安全确认错误状态：

- 保存失败：保留当前选择在页面内，显示重试；
- 支持资源读取失败：显示未配置或暂时不可用，并提供联系可信任的人和当地紧急服务的通用提示；
- 网络中断：不显示已保存，不允许绕过确认进入完整结果；
- 会话失效：清除安全确认选择，要求重新登录后重新确认。

## 5. 支持资源和历史流程

### 5.1 支持资源页

触发条件：今日页、结果页、我的页面或安全确认页点击支持资源。

步骤：

1. 显示当前环境可用资源的更新时间。
2. 按现在可以联系谁、校内支持、紧急支持分组。
3. 用户点击电话、复制信息或打开链接前，显示必要的确认。
4. 用户可以返回上一页，不要求先完成任何问卷。

成功状态：资源内容来自已配置资源库，展示来源或更新时间。

错误状态：

- 资源为空：显示暂未配置，不编造联系人和号码；
- 资源过期：显示资源可能已更新的提醒和重新加载；
- 链接不可用：保留文本、电话和返回入口；
- 网络失败：显示已缓存且标记为缓存的安全资源，否则显示未配置说明。

### 5.2 历史记录页

触发条件：我的 → 历史记录，或结果页点击查看历史。

页面分为心情记录、自测记录和睡眠观察三个区域。只显示当前用户自己的记录。

步骤：

1. 用户选择记录类别。
2. 系统按时间倒序加载记录。
3. 用户点击一条记录查看详情。
4. 用户可以删除单条记录，二次确认后提交。

成功状态：删除后该条从列表消失，并显示删除完成；分页位置自动修正。

错误状态：

- 没有记录：显示空状态和开始一次观察的入口；
- 删除失败：该条仍保留并显示未删除；
- 版本冲突：重新读取列表，不覆盖其他设备上的最新状态；
- 无权限：不显示记录内容，回到我的页面。

## 6. 树洞流程

### 6.1 树洞首页

触发条件：点击底部“树洞”。

步骤：

1. 加载允许公开或保护展示的匿名帖子。
2. 用户浏览列表，可以切换已经确认的排序入口。
3. 点击帖子进入详情。
4. 点击发布按钮进入发布页。

成功状态：列表显示匿名身份、正文摘要、公开状态和回应数量等允许展示字段。

空状态：显示“这里还没有可以展示的内容”，提供发布入口和社区规则入口。

错误状态：

- 加载失败：保留发布入口，显示重新加载；
- 内容暂不可见：显示状态说明，不暴露审核原因或其他用户信息；
- 会话失效：公开浏览可以按产品配置继续，但发布、回应和我的内容需要重新登录。

### 6.2 树洞发布页

触发条件：点击树洞首页发布按钮，或从我的内容进入新建。

前置条件：基础服务同意、社区同意和必要身份核验均已完成。

导航路径：树洞首页 → 发布页 → 内容检查提示 → 提交确认 → 帖子状态页。

步骤：

1. 页面说明匿名展示、内容检查、延迟公开、撤回和删除规则。
2. 用户输入正文。
3. 用户点击“继续检查”。
4. 系统执行固定规则检查，并在需要时创建人工审核任务。
5. 用户查看检查提示，确认提交。
6. 服务端创建帖子并返回检查中、等待确认或直接保护展示等状态。

成功状态：页面显示帖子编号对应的用户可见状态和下一步；检查中的帖子不出现在公开列表。

错误状态：

- 未同意社区规则：跳转数据与隐私页完成同意；
- 未核验身份：跳转身份核验；
- 正文为空或超出长度：定位输入框并显示规则；
- 固定规则阻断：显示需要修改的中性提示，不显示内部关键词；
- 网络失败：正文保留在当前页面内，不显示提交成功；
- 重复提交：返回同一帖子状态，不生成重复帖子；
- 账户停止使用：显示恢复账户入口。

### 6.3 帖子状态页

帖子可能处于以下用户可见状态：检查中、已公开、保护展示、等待确认、未公开、安全优先、已删除。

- 检查中：正在进行规则或人工检查，其他人不可见。
- 已公开：可在树洞列表中按规则展示。
- 保护展示：已脱敏或限制互动后展示。
- 等待确认：需要用户确认某项内容或支持说明。
- 未公开：不会出现在公开列表，可查看修改或删除入口。
- 安全优先：优先进行支持处理，通常不显示完整正文。
- 已删除：只显示删除事实，不恢复正文。

旧的“暂停”“拒绝”等说法不作为用户最终状态；工作人员的内部理由只用于任务和审计。

### 6.4 帖子详情页

触发条件：点击公开或保护展示的帖子，或从我的内容进入自己的帖子。

步骤：

1. 加载帖子允许展示的正文、匿名身份和状态。
2. 用户可以查看允许展示的回应。
3. 社区同意有效时，用户可以点击回应并提交文字。
4. 用户可以从自己的帖子进入撤回或删除确认。

成功状态：回应提交后显示处理中或已展示状态；自己的帖子状态与服务端一致。

错误状态：

- 帖子已删除或未公开：显示状态说明，不显示正文；
- 回应入口被禁用：说明社区同意已撤回或帖子不接受回应；
- 回应提交失败：保留未提交文字，不显示成功；
- 并发变化：刷新帖子状态并变为只读。

### 6.5 撤回和删除自己的内容

触发条件：我的内容或帖子详情点击撤回、删除。

步骤：

1. 显示动作影响范围和不可恢复内容。
2. 用户二次确认。
3. 服务端校验作者、状态和对象版本。
4. 成功后显示已撤回或已删除。

错误状态：对象已经被工作人员处理时，显示当前状态并不覆盖；无权限时不显示其他人的内容。

## 7. 我的模块流程

### 7.1 我的首页

触发条件：点击底部“我的”。

页面入口：匿名身份、历史记录、支持资源、我的内容、数据与隐私、停止使用账户。

成功状态：只显示当前账户的摘要和功能入口，不显示姓名、学号或后台处理信息。

错误状态：账户信息加载失败时保留静态入口，并显示重新加载；会话过期时不显示私密摘要。

### 7.2 匿名身份页

触发条件：我的 → 匿名身份。

页面显示当前匿名身份的展示名、用途和更换规则说明，不显示真实姓名和学号。匿名身份由服务端生成，学生不能把真实身份设置为公开昵称。

成功状态：显示当前匿名身份和“仅在树洞中展示”的说明。

错误状态：匿名身份未生成时显示创建中或重新加载，不显示内部标识；账户停止使用时显示恢复入口。

### 7.3 我的内容页

触发条件：我的 → 我的内容。

步骤：

1. 系统读取当前用户作为作者的帖子。
2. 列表显示状态、创建时间和允许的摘要。
3. 用户点击一条进入详情或管理。
4. 用户可以撤回或逐条删除。

空状态：显示尚未发布内容和去树洞发布入口。

错误状态：读取失败显示重试；单条操作发生版本冲突时显示最新状态，不覆盖后台决定。

### 7.4 数据与隐私页

触发条件：我的 → 数据与隐私。

页面分为：基础服务同意、社区内容同意、字段用途、单条删除入口、社区同意撤回/重新同意和停止使用账户。

用户可以撤回社区同意。撤回成功后仍可浏览公开树洞，但发帖和回应入口立即禁用；已有帖子不自动删除。基础服务同意不提供单独的即时撤回按钮，用户需要使用停止使用账户流程。

### 7.5 停止使用账户

触发条件：我的 → 数据与隐私 → 停止使用账户。

步骤：

1. 页面说明停止使用会限制新记录、发布和回应，并进入 30 天恢复期。
2. 用户输入确认词或完成二次确认。
3. 服务端记录停止时间、恢复截止时间和账户状态。
4. 页面退出私密功能，显示恢复入口。

成功状态：账户变为恢复期，新的观察和社区动作被拒绝；审计必要记录按规定保留。

错误状态：确认不匹配不提交；网络失败不显示停止成功；账户已经停止时显示恢复期事实。

### 7.6 恢复账户

触发条件：账户处于 30 天恢复期，用户重新登录并点击恢复。

步骤：验证会话 → 显示恢复影响 → 用户确认 → 服务端恢复可用状态。

成功状态：恢复观察、历史查看、发布和回应能力，但不会自动恢复已删除内容。

错误状态：恢复期已结束时说明无法恢复并展示新的首次使用路径；会话失效时要求重新登录。

## 8. 后台流程

### 8.1 后台登录页

页面编号：W-01。

页面编号：后台登录页。

触发条件：工作人员访问后台地址，或后台会话过期。

页面显示固定账号名称“心理健康中心工作人员 / 超级管理员”，不显示角色选择。

步骤：

1. 工作人员输入由环境配置提供的登录凭据。
2. 点击登录。
3. 服务端验证凭据、环境和账号能力。
4. 成功后进入工作台。

成功状态：进入工作台，并显示当前会话有效期和账号名称。

错误状态：凭据错误、账号禁用或环境不匹配都显示统一的登录失败；不透露账号是否存在。会话过期后返回登录页并清除详情缓存。

### 8.2 后台工作台

页面编号：W-02。

页面编号：工作台。

触发条件：后台登录成功，或点击后台导航的工作台。

页面固定包含三个区块：需要我处理、等待他人、最近处理。

步骤：

1. 系统读取当前账号有权看到的任务摘要。
2. 工作人员从需要我处理中打开任务。
3. 任务被认领后进入任务详情。
4. 等待他人中的任务只能查看必要状态。
5. 最近处理中的任务用于回看已经完成的事实。

成功状态：三个区块均能独立加载，某个区块失败不遮挡其他区块。

错误状态：

- 某区块加载失败：该区块显示重新加载；
- 无任务：显示空状态，不用虚构演示任务；
- 会话过期：清除任务详情，回到登录页；
- 无权限：不返回任务内容，只显示权限说明。

### 8.3 内容审核任务详情

页面编号：W-03。

触发条件：从工作台打开内容审核任务。

页面两栏：左侧为必要事实与脱敏内容，右侧为当前操作。

步骤：

1. 工作人员查看帖子时间、当前状态、脱敏正文和规则检查摘要。
2. 点击认领，服务端锁定任务版本。
3. 选择公开、保护展示、暂不公开或转安全复核。
4. 必要时填写内部处理理由，理由不展示给学生。
5. 点击提交。
6. 服务端校验权限、对象版本、任务状态并写入审计。

成功状态：显示新的帖子状态、操作时间和请求编号；任务移入最近处理。

错误状态：并发冲突、状态已变化、无权限、会话过期和提交失败均进入对应状态变体，不显示成功假象。

### 8.4 W-04 状态变体

页面编号：W-04。

以下状态变体可以出现在任何任务详情中，但必须优先展示当前事实并阻止覆盖：

- 并发冲突：对象版本已经变化，详情变为只读并提供重新读取；
- 状态已变化：任务已经被处理，显示当前状态，不允许再次提交旧动作；
- 无权限：只显示无权限说明，不返回受限正文、答案或身份字段；
- 会话过期：清除详情中的敏感内容，回到后台登录页；
- 提交失败：保留未提交的非敏感操作内容，显示重试，不显示成功；
- 加载失败或空数据：保留页面结构，显示重新加载或空状态。

### 8.5 安全支持任务详情

页面编号：安全支持任务变体，归属 W-03；异常状态归属 W-04。

触发条件：演示或明确授权环境创建最小安全支持任务。


步骤：

1. 工作人员只查看最小安全事实和已展示资源，不查看无关完整答案、身份和历史。
2. 认领任务。
3. 记录已完成的支持动作、现实联系人状态或下一次跟进事实。
4. 提交并写入审计。

成功状态：任务显示已处理或待跟进，并明确下一动作。

错误状态：资源未配置、权限不足、版本冲突或提交失败时保留只读事实，不允许扩展查看范围。

### 8.6 身份授权任务详情

页面编号：身份授权任务变体，归属 W-03；异常状态归属 W-04。

触发条件：出现明确的身份查看申请，并且任务具备授权范围。

步骤：

1. 查看申请理由、申请范围、对象引用、有效期和当前状态。
2. 认领任务。
3. 允许、拒绝或要求补充事实。
4. 如允许，只在限定时间和限定字段范围内显示身份字段。
5. 提交并写入每一次读取审计。

成功状态：授权决定和到期时间清晰显示；超期自动失效。

错误状态：无授权范围、已过期、并发变化或会话过期时不显示身份字段。

### 8.7 跟进任务详情

页面编号：跟进任务变体，归属 W-03；异常状态归属 W-04。

触发条件：已经存在需要记录后续事实的任务。

步骤：查看最小事实 → 认领 → 填写事实性跟进记录 → 提交 → 进入最近处理或等待他人。

跟进记录只能描述已发生的联系、资源提供、下一次约定或未联系结果，不填写诊断、主观标签或模型建议。

### 8.8 W-05 审计日志页

页面编号：W-05。

触发条件：后台导航进入审计与演示。

步骤：

1. 选择时间、对象类型、动作或结果筛选。
2. 读取只读审计条目。
3. 查看操作者、内部对象引用、动作、结果、时间和请求编号。

成功状态：列表可分页，条目不可编辑或删除。

错误状态：筛选失败显示重试；权限不足时不加载日志内容；会话过期返回登录页。

### 8.9 W-05 演示数据重置

页面编号：W-05。

触发条件：工作人员在演示环境的审计与演示页点击重置。

步骤：

1. 页面明确显示当前环境名称和将被重置的演示范围。
2. 工作人员二次确认。
3. 服务端校验当前环境必须是演示环境。
4. 清理演示集合并写入固定演示数据。
5. 返回每个数据域的成功或失败结果。

成功状态：所有演示域均显示重置完成，并生成审计记录。

错误状态：

- 真实环境：服务端拒绝，不能开始删除；
- 部分失败：逐域显示失败，不能显示整体成功；
- 并发操作：保留当前任务事实并要求重新读取；
- 会话过期：不执行重置。

## 9. 后台宽度和显示流程

标准画布为 1440×900，最小工作宽度为 1280px。

- 宽度大于或等于 1280px：显示完整桌面工作台。
- 宽度低于 1280px：显示“请使用电脑浏览器访问管理后台”，不压缩为移动端布局。
- 浏览器高度不足：页面纵向滚动，不能把详情栏缩到不可操作。
- 桌面网络中断：保留页面骨架和非敏感已读摘要，操作按钮进入不可用状态。

## 10. 全流程验收路径

### 10.1 普通学生路径

首次打开 → 同意基础服务 → 微信登录 → 今日 → 保存心情 → 自测中心 → 完成 GAD-7 → 普通或较高分数结果 → 支持资源 → 我的 → 历史记录。

预期：所有记录仅本人可见，服务端计算结果，AI 不参与评分。

### 10.2 安全支持路径

自测中心 → PHQ-9 → 第 9 题非零 → 安全确认 → 选择“我不太确定”或“我现在无法保证安全” → 支持资源 → 离开或按允许路径继续。

预期：先显示支持资源，不显示不必要的分数、历史、身份和模型内容；只有演示或明确授权环境生成最小安全任务。

### 10.3 匿名社区路径

树洞 → 同意社区内容 → 身份核验 → 发布 → 内容检查 → 检查中或保护展示 → 帖子详情 → 回应；随后我的 → 数据与隐私 → 撤回社区同意。

预期：撤回后仍可浏览，但发帖和回应被禁用；已有内容不自动删除。

### 10.4 后台处理路径

后台登录 → 工作台 → 需要我处理 → 内容审核详情 → 认领 → 保护展示 → 最近处理 → 审计日志。

预期：详情按两栏显示，处理只返回脱敏必要事实，动作写入审计；并发冲突时页面只读。
