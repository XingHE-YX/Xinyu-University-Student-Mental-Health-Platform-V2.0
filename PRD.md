# 心语 V2 产品需求文档

## 0. 文档信息

- 文档版本：PRD-1.0
- 编写日期：2026-08-28
- 产品名称：心语 V2
- 产品形态：大学生心理健康自助观察与匿名轻社区微信小程序
- 配套后台：独立桌面 Web 管理后台
- 文档状态：进入开发前的产品基线
- 本文作用：将已经收口的产品、界面、数据和安全决策整理为可实现、可验收的产品合同
- 规范优先级：本文与根目录其余五份规范共同组成当前标准；如果历史文档出现不同说法，以本轮六份规范和最新 V2 文档为准，不回溯恢复旧决策

关联文档：

- [应用流程文档](APP_FLOW.md)
- [技术栈文档](TECH_STACK.md)
- [前端指南](FRONTEND_GUIDELINES.md)
- [后端结构文档](BACKEND_STRUCTURE.md)
- [实施计划文档](IMPLEMENTATION_PLAN.md)
- [V2 已确认产品决策](docs/v2/V2_CONFIRMED_PRODUCT_DECISIONS.md)
- [V2 页面与导航设计](docs/superpowers/specs/2026-08-26-page-inventory-navigation-design.md)
- [V2 范围冻结与演示边界](docs/superpowers/specs/2026-08-26-v2-scope-freeze-demo-boundary.md)

## 1. 产品定义

心语 V2 为在校大学生提供一个低压力、可匿名、以自我观察为核心的微信小程序。用户可以记录当天的心情，完成 PHQ-9、GAD-7 和睡眠观察，查看自己的历史记录，在树洞中阅读或发布内容，并在需要时看到清晰、克制的支持资源。

产品不替代医疗机构、心理咨询、危机干预或学校正式管理系统。它不做诊断，不根据模型输出判断风险，不把学生变成供后台浏览的内容流，也不把心理健康体验包装为新闻资讯或游戏化打卡。

后台是独立的桌面 Web 工作台，仅供经过授权的心理健康中心工作人员处理内容审核、安全支持、身份授权和跟进事实。后台不放入移动端底部导航，也不向工作人员提供不必要的完整答案、身份信息或社区浏览权限。

## 2. 产品目标与非目标

### 2.1 V2 目标

1. 让学生在 3 分钟左右完成一次低负担的自我观察。
2. 让用户能够看懂自己的记录，但不会被单一分数或颜色标签定义。
3. 让用户能够在匿名前提下阅读和表达，同时保留明确的内容检查与安全边界。
4. 在出现安全支持需求时，优先展示现实可用的支持资源，并减少不必要的信息暴露。
5. 让工作人员通过任务工作台处理必要事实，所有敏感读取和状态变化可追溯。
6. 在演示环境中使用可复现的合成数据，确保演示数据与真实数据完全隔离。

### 2.2 V2 不做的事情

- 不做医疗诊断、心理疾病判定、危机等级自动判断或治疗建议。
- 不用 AI 计算 PHQ-9、GAD-7 或睡眠观察结果。
- 不由 AI 单独决定是否公开帖子、是否联系用户或是否启动安全处置。
- 不在客户端直连 DeepSeek、数据库管理接口或后台接口的高权限能力。
- 不提供移动端管理后台、新闻资讯首页、关注/粉丝体系、私信和社交排行榜。
- 不提供结果导出、批量删除、一键清空、申诉中心或社区评论点赞体系。
- 不在运行时抓取网络名言，不由 AI 生成每日短句，不做个性化推荐。
- 不把视觉预览文件中的不可点击装饰按钮当成真实功能缺口。

## 3. 用户、角色与使用边界

### 3.1 学生用户

主要使用微信小程序的在校大学生。学生可以：

- 查看今日内容和每日短句；
- 保存每日心情；
- 完成三类自测并查看自己的结果和历史；
- 浏览树洞；
- 在完成社区同意后匿名发帖和回应；
- 查看、撤回或删除自己的树洞内容；
- 查看支持资源；
- 管理数据使用同意和停止使用账户。

学生不能：

- 查看其他人的真实身份；
- 查看后台任务、审计日志或内部审核理由；
- 修改服务端计算出的分数、结果、公开状态或安全状态；
- 从客户端获取 DeepSeek 密钥或后台能力。

### 3.2 心理健康中心工作人员

使用独立桌面 Web 管理后台。首版采用固定预设账号展示：

- 显示名称：心理健康中心工作人员
- 管理能力：超级管理员
- 不提供角色选择或角色切换

该账号只是 V2 的明确演示和最小后台入口。真实环境必须把账号凭据放在服务端配置或学校认可的身份系统中，不能写入前端包。

### 3.3 系统角色

系统负责执行固定规则、数据隔离、状态迁移、并发保护、审计写入和资源展示。系统不以 AI 输出替代工作人员判断。

### 3.4 AI 服务

DeepSeek-V4-Flash-0731 只作为受限的文字辅助能力，服务端在字段白名单、去身份化和文本遮罩后调用。AI 可以生成中性、非诊断性的文字建议或辅助摘要，但不能评分、判定风险、诊断或执行安全处置。

## 4. 信息架构

### 4.1 移动端底部导航

固定为四项，顺序不可改变：

1. 今日
2. 自测
3. 树洞
4. 我的

底部导航只存在于学生端主流程页面。首次使用、答题、结果、安全支持、发布和确认弹层可暂时隐藏或禁用导航，防止用户在未完成关键动作时丢失状态。

### 4.2 移动端页面

| 页面编号 | 页面名称 | 入口 | 主要目的 |
| --- | --- | --- | --- |
| S-01 | 今日 | 首次完成后默认进入、底部导航 | 查看短句、今日心情与快捷入口 |
| S-02 | 自测 | 底部导航、今日快捷入口 | 选择并开始三类自测 |
| S-03 | 树洞 | 底部导航 | 浏览匿名内容、进入发布 |
| S-04 | 我的 | 底部导航 | 查看历史、支持资源、匿名身份和隐私 |
| S-05 | 首次使用 | 首次打开或同意未完成 | 说明产品边界、基础服务同意、社区同意 |
| S-06 | 身份核验 | 需要私密保存或发布前 | 完成学校身份核验，建立不可逆内部引用 |
| S-07 | 自测答题 | 从自测中心开始 | 按固定题目作答并保存会话 |
| S-08 | 自测结果 | 完成自测后 | 展示普通、较高分数或安全支持结果 |
| S-09 | 支持资源 | 结果页、我的、今日 | 展示现实支持方式 |
| S-10 | 历史记录 | 我的 | 查看个人自测和心情记录 |
| S-11 | 树洞发布 | 树洞右上角或空状态按钮 | 编写、检查和提交匿名内容 |
| S-12 | 树洞详情 | 树洞列表 | 查看正文、回应和帖子状态 |
| S-13 | 数据与隐私 | 我的 | 查看同意、字段用途、删除与停止使用 |
| S-14 | 我的内容 | 我的 | 管理自己发布的内容和状态 |
| S-15 | 匿名身份 | 我的 | 查看当前匿名身份，不展示真实身份 |

### 4.3 后台页面

| 页面编号 | 页面名称 | 主要目的 |
| --- | --- | --- |
| W-01 | 工作台入口 | 固定账号登录、会话状态和工作区入口 |
| W-02 | 任务工作台 | 通过三个任务区块分流待处理工作 |
| W-03 | 任务详情 | 查看最小必要事实并完成当前操作 |
| W-04 | 状态变体 | 呈现并发冲突、无权限、会话过期、提交失败等事实 |
| W-05 | 审计与演示 | 只读查看审计，重置演示数据 |

## 5. 核心产品原则

### 5.1 观察而非定义

分数是一次问卷在固定规则下的观察结果，不是用户身份、人格或医学结论。页面优先使用“这次记录”“最近的回答”“可以进一步留意”等表达，不使用“你就是某种人”或“你一定存在某种问题”。

### 5.2 支持优先

安全支持状态下先给出联系可信任的人、校内心理中心、当地急救或危机资源等现实支持路径。安全状态页面不显示不必要的分数、历史、AI 内容、真实身份或风险等级标签。

### 5.3 最小必要暴露

学生端只返回完成当前页面所需的字段；后台根据任务能力返回必要事实和脱敏内容。姓名、学号、完整答题、未脱敏正文和安全确认内容不能因“方便查看”而整包返回。

### 5.4 匿名不等于无规则

树洞使用匿名身份展示，但发布前必须完成社区同意和内容检查。匿名身份与真实身份在数据域上分离，只有明确的授权任务才能在限定范围和有效期内查看身份必要字段。

### 5.5 安全状态由固定规则和人工流程决定

PHQ-9 第 9 题非零时进入安全确认分支。模型不读取完整安全确认内容，不负责分流，也不替代工作人员或现实支持体系。

## 6. 移动端功能需求

### M-01 首次使用与同意

触发条件：用户首次打开、基础服务同意未完成，或社区同意尚未完成但用户进入发布流程。

必须支持：

- 首次使用的固定路径为“先从今天开始 → 先确认这是你的记录 → 今日”；基础服务同意和身份核验完成后才能进入今日；

- 说明产品是自我观察和匿名轻社区，不是医疗诊断工具；
- 展示基础服务同意的用途、保存范围和停止使用入口；
- 单独展示社区内容同意，说明发帖、回应、内容检查和公开展示边界；
- 提供“同意并继续”和“暂不使用”两个清晰动作；
- 记录同意版本、时间、来源和撤回状态；
- 基础服务未同意时不能进入需要账户的功能；
- 社区同意撤回后可以继续浏览，但不能发帖或回应，已有内容不会自动删除。

### M-02 微信登录、身份核验与匿名身份

触发条件：首次使用进入今日前，或后续需要保存私密记录、查看历史、发布树洞或执行数据管理而身份尚未核验时。

规则：

- 使用微信登录建立服务端会话，客户端不保存学校密钥或后台凭据；
- 身份核验由服务端适配学校核验方式，核验不可用时不得伪造成功；
- 真实姓名和学号只进入身份域，并通过内部不可逆引用与业务域关联；
- 树洞展示昵称由匿名身份域生成，匿名身份不能反推出姓名和学号；
- 核验完成后仍不在学生端公开真实身份；
- 会话过期时隐藏敏感数据并要求重新登录。

### M-03 今日

今日页至少包含：

- 一条项目自有短句；
- 今日心情入口和已保存状态；
- 自测入口及各模块最近一次记录状态；
- 支持资源入口；
- 不强制连续打卡，不显示公开排行榜。

短句按本地或后端版本化库提供，每天最多展示一条主短句。展示格式为“短句——作者 / 作品”；项目原创句显示“——心语 V2”。运行时不联网抓取、不让模型生成、不做个性化推荐。

### M-04 每日心情

用户可以选择当天的主观心情并首次保存。保存后当天不提供修改入口；心情是私密观察记录，不进入树洞审核，不自动创建后台任务，不用于推断诊断或风险。

成功状态：显示已保存时间和当前选择；当天再次进入只显示已记录事实，不提供修改入口。

错误状态：网络失败时保留本地未提交选择并显示“暂时没有保存成功，请稍后重试”；不得把未提交状态误显示为已保存。

### M-05 自测中心

自测中心固定展示三个模块：

1. PHQ-9：近两周情绪与兴趣相关的九题标准量表。
2. GAD-7：近两周焦虑相关的七题标准量表。
3. 睡眠观察：八道项目自定义观察题，不宣称是医学量表，不计算临床总分。

每个模块展示用途、预计耗时、最近记录时间和开始入口。模块之间不合并总分，不生成跨量表排名或综合风险分。

### M-05A 固定结果规则摘要

PHQ-9 使用 9 个计分题，每题 0 至 3 分，总分 0 至 27；影响题单独保存，不计入总分。当前固定的非诊断性表达为：

| 总分 | 固定表达 |
| --- | --- |
| 0–4 | 近期情绪相关信号较少 |
| 5–9 | 近期有一些情绪困扰 |
| 10–14 | 近期困扰较明显，可优先查看支持资源 |
| 15–19 | 近期困扰较重，建议尽快了解专业支持 |
| 20–27 | 近期困扰很重，建议尽快获得专业支持 |

GAD-7 使用 7 个计分题，每题 0 至 3 分，总分 0 至 21。当前固定的非诊断性表达为：

| 总分 | 固定表达 |
| --- | --- |
| 0–4 | 近期紧绷和担心的信号较少 |
| 5–9 | 近期有一些紧绷或担心 |
| 10–14 | 近期焦虑相关困扰较明显，可优先查看支持资源 |
| 15–21 | 近期困扰较重，建议尽快与专业人员交流 |

睡眠观察固定为最近 7 天的 8 题自定义观察，不计算总分。结果只输出三个维度：作息节律由第 1 题定位；睡前阻力取第 3、4、7 题中最需要关注的位置，第 8 题只补充原因；恢复与日间影响取第 5、6 题中最需要关注的位置。第 2 题只作为实际睡眠时长事实补充，第 8 题可以选择跳过或“不想回答”。具体题干、选项和来源见 [医学依据文档](docs/v2/V2_ASSESSMENT_MEDICAL_EVIDENCE_AND_REFERENCES.md)；不得将睡眠观察称为医学量表或生成临床等级。

### M-06 答题会话

答题页面必须支持：

- 一次只呈现当前问题，显示进度但不显示风险颜色；
- 固定题目、固定选项、固定计分规则由服务端版本控制；
- 允许返回修改已答题；
- 离开前提示未完成状态；
- 网络中断时保留未完成会话的本地临时状态，但未提交内容不进入历史；
- 完成提交使用幂等键，重复点击不能生成重复结果。

PHQ-9 第 9 题选择非零后立即进入安全确认，在确认后返回答题页或进入支持流程；未完成整份问卷前不生成结果。

### M-07 结果页

结果页有三类产品状态：

#### 普通结果

- 固定规则结果先于 AI 内容显示；普通和较高分数结果可以显示可选的 AI 辅助说明；
- AI 辅助说明加载失败时使用固定规则说明，不阻塞结果、历史或支持资源；

- 展示本次模块分数或睡眠观察摘要；
- 展示分数含义和非诊断说明；
- 提供温和的下一步建议和支持资源入口；
- 可进入历史记录；
- 不显示红黄绿风险区。

#### 较高分数结果

- 可在固定结果和支持资源之后显示可选的 AI 辅助说明；
- AI 不得改变分数、参考分层、支持优先级或页面状态；

- 使用固定规则判断“较高分数”展示分支；
- 用中性文字提醒用户考虑联系可信任的人或专业支持；
- 保留查看结果依据和支持资源的入口；
- 不使用疾病标签，不由 AI 解释为诊断；
- 不自动创建安全任务，除非同时满足安全分支条件。

#### 安全支持状态

- 不调用 AI，也不显示历史结果中保存的 AI 辅助文字；

- 用户在 PHQ-9 答题过程中将第 9 题选择为非零时立即触发；
- 安全确认选择“我现在无法保证安全”时不继续显示完整结果；
- 优先展示现实支持资源、离开页面和修改回答入口；
- 在允许继续但已完成安全支持确认的情况下，结果可以保存为受限安全状态；
- 安全状态不显示不必要的分数、历史、AI、真实身份或风险等级。

### M-08 安全确认

安全确认使用原文固定的三路选择：

- 我现在可以保证安全：返回答题页继续完成剩余题目；全部完成后生成普通或较高分数结果，不创建后台安全任务；
- 我不太确定：先展示支持资源；用户主动继续后返回答题页，全部完成后记录受限安全状态；在演示或明确授权模式下生成最小安全支持任务；
- 我现在无法保证安全：展示支持资源、离开和修改入口，不继续显示完整结果；在演示或明确授权模式下只生成最小安全支持任务。

安全确认不得出现诊断、责备、强制承诺、无依据的保证或模型生成内容。

### M-09 支持资源

资源按“现在可以联系谁”“校内支持”“紧急支持”分组，具体联系人、电话、时间和链接由环境配置提供。资源缺失时显示“暂未配置，请联系可信任的人或当地紧急服务”，不得生成虚构电话。

用户每次打开安全支持资源页都能看到退出和回到上一步的入口。系统可以记录已展示资源的版本，但不把点击行为解释为用户已经安全。

### M-10 树洞浏览

树洞首页提供：

- 匿名帖子列表；
- 按最新、推荐展示等已确认的固定入口；
- 帖子状态提示；
- 空状态、加载失败、无网络和内容暂不可见状态；
- 发布入口。

公开列表只返回允许公开或保护展示的脱敏内容。检查中、等待确认、未公开、安全优先和已删除内容不进入其他用户的公开列表。

### M-11 树洞发布与内容检查

发布步骤固定为：打开发布页、编写正文、查看内容检查提示、确认提交、等待状态。

- 发布前告知匿名展示、内容检查、可能延迟公开和撤回方式；
- 必须完成社区同意；
- 文字先经过固定规则检查，必要时由后台工作人员处理；
- 检查中状态不显示为已公开；
- 内容决定只有公开、保护展示、暂不公开、转安全复核四种最终操作；
- 用户可以撤回自己的待处理内容，并可以删除自己的已公开或保护展示内容；
- 不公开原因只用于内部处理和审计，不向社区展示为羞辱性标签。

### M-12 树洞详情与回应

详情页展示正文、匿名身份、公开状态和已允许展示的回应。用户可以在社区同意有效时回应；撤回社区同意后仍可查看公开内容，但回应入口被禁用并说明重新同意路径。

安全优先或非公开状态的帖子只展示必要的状态说明，不展示内部审核记录、工作人员备注或真实身份。

### M-13 我的、历史与内容管理

我的页面包含匿名身份、历史记录、支持资源、我的内容、数据与隐私和停止使用账户。

- 历史仅本人可见；
- 自测结果支持单条删除；
- 心情记录支持单条删除；
- 我的内容支持查看公开、保护展示、检查中、等待确认、未公开、安全优先和已删除状态；
- 删除动作必须二次确认并明确不可恢复范围；
- 不提供一键清空或批量删除；
- 停止使用账户进入 30 天恢复期，期间不接受新记录、发帖或回应；
- 恢复期结束后按数据保留规则执行删除或不可识别化；
- 账户停止使用不等同于后台实时抹除审计必要记录。

### M-14 社区同意撤回

入口：我的 → 数据与隐私 → 数据使用与同意。

撤回后：

- 可以浏览公开树洞；
- 不能发帖、回应或重新提交社区内容；
- 已有帖子不会自动删除；
- 用户仍可进入我的内容逐条删除；
- 重新同意后恢复发布和回应能力；
- 撤回、重新同意和相关版本均记录时间与版本。

## 7. 后台工作台需求

### W-01 固定账号与入口

登录页只显示固定预设账号信息“心理健康中心工作人员 / 超级管理员”，不显示角色选择器。登录成功后进入工作台；登录失败不透露账号是否存在；会话过期后清除敏感详情并返回登录页。

### W-02 三任务区块

工作台固定使用三个区块：

1. 需要我处理：当前账号可以领取或继续处理的任务。
2. 等待他人：已分配给其他工作人员或等待外部条件的任务，只读。
3. 最近处理：当前账号最近完成或变更过的任务，用于回看事实。

任务卡必须显示任务类型、最小事实摘要、状态、更新时间、是否已被认领和进入详情的动作。不得在列表中直接展示完整正文、完整答案或身份字段。

### W-03 两栏任务详情

详情页固定为两栏：

- 必要事实与脱敏内容：任务来源、时间、状态、脱敏正文或最小安全事实、已有处理记录；
- 当前操作：认领、释放、公开、保护展示、暂不公开、转安全复核、记录安全支持、提交身份授权或记录跟进事实。

任务类型固定支持内容审核、安全支持、身份授权、跟进四种变体。每种变体只显示自己的字段和动作。

### W-04 状态变体

必须实现并验收以下状态：

- 并发冲突：对象版本已变化，页面变为只读并要求重新读取；
- 状态已变化：任务已被处理，保留当前事实，不允许覆盖；
- 无权限：只显示无权限说明，不返回受限字段；
- 会话过期：清除敏感详情，保留必要的未提交操作提示；
- 提交失败：保留用户已填写的非敏感操作内容，明确重试，不显示成功；
- 空数据和加载失败：保留布局和重新加载入口。

### W-05 审计与演示数据

审计页面只读，支持按时间、对象类型、动作和结果筛选。审计内容显示操作者、对象内部引用、动作、结果、时间和请求编号，不复制完整正文、完整答题或不必要的真实身份。

演示数据重置只能作用于演示环境，并要求二次确认。重置结果必须可验证地显示成功、失败或部分失败；不得触碰真实环境。

## 8. 权限和数据边界

| 数据 | 学生本人 | 其他学生 | 后台任务 | AI |
| --- | --- | --- | --- | --- |
| 姓名、学号 | 核验时可输入 | 不可见 | 仅身份授权任务的最小字段 | 不可见 |
| 每日心情 | 可读、可删 | 不可见 | 默认不可见 | 不可见 |
| 完整自测答案 | 本人可在结果范围内查看 | 不可见 | 默认不可见 | 不可见 |
| 自测分数 | 本人可见 | 不可见 | 仅必要任务 | 不发送 |
| 树洞正文 | 作者按状态可见 | 仅公开投影 | 脱敏后按任务可见 | 遮罩片段或不发送 |
| 安全确认 | 本人按流程可见 | 不可见 | 最小安全事实，需授权 | 不可见 |
| 审计记录 | 不可见 | 不可见 | 只读按能力可见 | 不可见 |

所有返回字段都由服务端按用户身份、环境、任务类型、对象状态和数据版本裁剪。客户端自行伪造字段不能改变权限或状态。

## 9. 统一错误和空状态要求

每个页面都必须明确区分：加载中、空数据、无权限、未同意、会话过期、网络失败、服务端失败、并发冲突和提交成功。按钮在请求进行中禁用，重复提交使用幂等键。

错误文案必须：

- 说明用户下一步能做什么；
- 不暴露数据库、密钥、内部规则或其他用户信息；
- 不把系统失败归因于用户；
- 安全状态下不使用可能增加恐慌的红色大面积告警。

## 10. 可访问性与界面验收

- 主要可点击热区不小于 44×44px；
- 正文、题目和支持资源不以颜色作为唯一含义；
- 字体放大后允许页面纵向增长，不截断关键说明；
- 所有表单错误与按钮状态有文字说明；
- 支持读屏语义、合理焦点顺序和键盘操作的后台 Web；
- 手机端适配 360px 至 430px 常见宽度；后台标准画布 1440×900，1280px 为最小工作宽度；
- 低于 1280px 的后台显示“请使用电脑浏览器”，不压缩为移动布局。

## 11. 产品验收口径

产品验收以关键路径完整和边界正确为准：

1. 新用户可以完成同意、微信登录和必要身份核验。
2. 用户可以保存今日心情，失败时不会看到虚假的成功状态。
3. PHQ-9、GAD-7、睡眠观察均能完成、计算和保存正确结果。
4. PHQ-9 第 9 题非零一定进入安全确认，安全分支不泄露多余信息。
5. 树洞发布一定经过社区同意、内容检查和明确状态展示。
6. 社区同意撤回后浏览可用，发布和回应不可用；已有内容不被自动删除。
7. 用户可以逐条删除记录和内容，停止使用账户有 30 天恢复期。
8. 后台能按三个任务区块工作，详情遵守两栏和四种任务变体。
9. 后台能正确处理并发冲突、无权限、会话过期和提交失败。
10. 演示重置不影响真实数据，审计只读且不复制不必要敏感内容。

## 12. 设计完成与外部配置项

当前产品设计不再保留待定功能。以下不是设计缺口，而是开发或上线前必须填写的外部配置：

- 微信小程序 AppID；
- CloudBase 演示和真实环境 ID；
- 后台域名与 HTTPS 来源；
- DeepSeek API Key；
- 学校身份核验接口或人工核验方案；
- 校内心理中心、联系人、时间和当地紧急支持资源；
- 真实环境账号、隐私说明、数据保存期限和学校授权材料。

以上配置未填写时，系统必须显示未配置状态或保持演示模式，不能用假值伪造真实可用。
