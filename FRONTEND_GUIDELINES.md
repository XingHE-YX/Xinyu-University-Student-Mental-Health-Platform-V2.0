# 心语 V2 前端指南

## 0. 适用范围

本指南同时约束学生端微信小程序和独立桌面 Web 管理后台。两端共享视觉气质，但不共享页面布局：学生端优先移动阅读和低压力操作，后台优先桌面任务处理和必要事实展示。

产品视觉基线为“暖雾纸白 × 静谧青”。界面应像安静、可靠的记录空间，不像新闻资讯、医疗仪表盘或游戏化打卡工具。

## 1. 视觉原则

1. 先让用户知道现在是什么状态，再告诉用户可以做什么。
2. 用文字层级、留白和分隔线组织信息，减少卡片堆叠。
3. 用单色青色阶梯表达观察趋势，不用红黄绿表达心理风险。
4. 安全支持状态优先显示现实资源，不堆叠分数、历史、模型内容和身份信息。
5. 每个动作都要有明确成功、失败、处理中和不可用状态。
6. 重要说明必须能读懂，不把含义只放在图标或颜色里。
7. 不用渐变、发光、厚重阴影、玻璃拟态、新闻式信息流和游戏化勋章。

## 2. 调色板

### 2.1 基础色

| 色彩变量 | 十六进制 | 用途 |
| --- | --- | --- |
| 暖雾纸白 | #F7F4EE | 移动端和后台主背景 |
| 内容白 | #FFFFFF | 内容区域、输入区域和对比表面 |
| 深墨绿灰 | #1F2D2B | 主标题、正文和关键数据 |
| 次级灰绿 | #5F6E6A | 次级说明、时间和辅助信息 |
| 弱化灰绿 | #8C9A96 | 占位、禁用和不重要说明 |
| 静谧青 | #2E6F68 | 主要按钮、当前导航、链接和主图形 |
| 深静谧青 | #245B55 | 按下、焦点和深色文字按钮 |
| 浅青表面 | #E8F0EC | 选择项、轻提示和次级背景 |
| 青灰分隔线 | #D7E4DF | 1px 分隔线、表格线和输入边界 |
| 安全砖红 | #B85C4A | 少量现实支持提醒和危险操作文字 |
| 浅安全砖红 | #F5E7E3 | 安全支持区域的浅色底 |
| 谨慎金 | #C58A3D | 必要的等待或注意提示，不表示风险等级 |
| 浅谨慎金 | #F6EEDB | 等待和配置提示底色 |

### 2.2 语义映射

| 语义 | 背景 | 文字/边框 | 禁止用法 |
| --- | --- | --- | --- |
| 普通可用 | 暖雾纸白或内容白 | 深墨绿灰 | 不用绿色表示“安全等级” |
| 主要行动 | 静谧青 | 内容白 | 不用于风险判断 |
| 选中 | 浅青表面 | 深静谧青 | 不仅依靠颜色识别 |
| 等待处理 | 浅谨慎金 | 谨慎金 | 不叫“中风险” |
| 安全支持 | 浅安全砖红 | 安全砖红 | 不铺满页面、不显示风险分级 |
| 禁止或删除 | 内容白 | 安全砖红 | 只用于明确的删除或现实支持提示 |
| 不可用 | 暖雾纸白 | 弱化灰绿 | 不降低文字到不可读 |

### 2.3 对比度规则

- 主文字与背景目标对比度至少达到普通正文可读标准，优先使用深墨绿灰。
- 弱化灰绿不得用于正文、题目或唯一的错误说明。
- 链接同时使用颜色和下划线或明确文字。
- 状态必须同时有文字、图标或结构差异；不得让用户只看颜色猜状态。

## 3. 字体和排版

### 3.1 字体栈

移动端和后台统一使用：

    -apple-system, BlinkMacSystemFont, PingFang SC, Hiragino Sans GB, Microsoft YaHei, sans-serif

数字和时间优先沿用系统无衬线字体，不单独引入网络字体，避免小程序加载延迟和字体替换跳动。

### 3.2 字号刻度

| 名称 | 字号 | 行高 | 用途 |
| --- | --- | --- | --- |
| 辅助 | 12px | 18px | 出处、次要时间、表格辅助信息 |
| 正文小 | 14px | 22px | 次级说明、按钮辅助文字 |
| 正文 | 16px | 26px | 普通正文、表单、列表 |
| 小标题 | 18px | 28px | 卡片或区块标题 |
| 页面标题 | 20px | 30px | 移动页面标题、后台区域标题 |
| 强调标题 | 24px | 34px | 结果摘要、重要状态 |
| 大标题 | 28px | 38px | 首次使用或后台主标题 |
| 特殊数据 | 32px | 42px | 结果数字或关键统计，只有必要时使用 |

正文默认 16px。结果数字不以大字号制造恐慌，不用大面积红色突出分数。

### 3.3 字重

- 常规正文：400。
- 区块标题：500。
- 页面标题和重要动作：600。
- 不使用 700 以上作为常规视觉层级。

## 4. 间距、尺寸和形状

### 4.1 间距刻度

所有间距只能从以下刻度选择：4px、8px、12px、16px、20px、24px、32px、40px、48px、64px。

| 场景 | 规定 |
| --- | --- |
| 手机页面左右边距 | 360px 和 375px 宽度使用 20px；393px 及以上使用 24px |
| 手机区块间距 | 24px 或 32px |
| 手机标题与正文 | 8px 或 12px |
| 按钮内边距 | 横向 16px 至 20px，纵向 12px |
| 后台页面左右边距 | 40px，宽屏可扩展但不改变主栏比例 |
| 后台栏间距 | 24px 或 32px |
| 表格行高 | 最低 52px，包含多行内容时纵向增长 |
| 主要热区 | 至少 44px × 44px |

### 4.2 圆角

- 页面和大区块：不强制圆角，优先使用留白和分隔线。
- 输入框、按钮和列表轻容器：8px。
- 小提示和标签：4px。
- 安全支持浅色区域：8px。
- 禁止超过 12px 的大圆角卡片，禁止胶囊化整页内容。

### 4.3 边框和阴影

- 默认边框为 1px 实线青灰分隔线。
- 不使用厚边框表达选中；选中可使用 2px 静谧青边框并保留文字说明。
- 默认不使用阴影。
- 弹层可以使用轻微阴影，但只用于建立层级，不用于装饰。
- 不使用发光、内阴影和连续卡片阴影。

## 5. 图标、图形和动效

### 5.1 图标

- 图标使用线性、低细节、圆角端点风格。
- 图标尺寸优先 20px 或 24px，触控热区扩大到 44px。
- 图标旁必须有文字，除非是通用返回、关闭或系统导航动作。
- 不使用带有医疗诊断含义的十字、心电图或警报图标作为普通状态。

### 5.2 图表

V2 只允许使用以下图形：分数定位尺、回答指纹、单模块历史轨迹、近期记录时间带和睡眠三维观察线。

- 使用静谧青的同色相阶梯；
- 不使用红黄绿风险区；
- 不做跨量表总分、雷达图、综合风险仪表盘；
- 图形必须有文本摘要和无图形降级状态；
- 图表没有数据时显示数据不足，不用空白画布伪装有趋势。

Lieflat Charts Skill 不属于当前前端依赖。只有在后续真正进入高级图表实现并得到明确要求后，才重新评估图表方案。

### 5.3 动效

- 页面切换 160ms 至 220ms，使用淡入或短距离位移。
- 提交处理中只使用轻量进度提示，不使用连续跳动或庆祝动画。
- 安全支持和较高分数结果不使用突然放大、震动或红色闪烁。
- 设备开启减少动效时，所有动画降级为即时状态切换。
- 动效不得阻塞返回、关闭或现实支持资源操作。

## 6. 学生端组件规范

学生端使用原生微信组件和项目内基础组件，不引入第三方 UI 组件库。

### 6.1 全局组件清单

| 组件 | 必须支持的状态 | 说明 |
| --- | --- | --- |
| 页面容器 | 正常、加载、错误 | 处理背景、左右边距和底部安全区 |
| 顶部标题栏 | 有返回、无返回、处理中 | 标题和返回行为清晰 |
| 底部导航 | 四项、当前项、禁用项 | 今日、自测、树洞、我的 |
| 主要按钮 | 默认、按下、处理中、禁用、错误 | 请求中不能重复提交 |
| 次要按钮 | 默认、按下、禁用 | 不抢主要动作层级 |
| 文本输入 | 空、输入中、错误、只读 | 显示长度和错误说明 |
| 单选选项 | 未选、已选、禁用、错误 | 不只用颜色表达选择 |
| 进度条 | 未开始、进行中、完成 | 只表示答题进度，不表示风险 |
| 状态文字 | 检查中、公开、保护、等待、未公开、安全优先、删除 | 状态文字必须来自服务端枚举 |
| 结果摘要 | 普通、较高分数、安全支持、数据不足 | 安全状态隐藏不必要字段 |
| 支持资源列表 | 已配置、未配置、加载失败 | 电话和来源清晰 |
| 树洞帖子 | 公开、保护、检查中、本人状态 | 非公开不泄露正文 |
| 底部弹层 | 普通确认、安全支持、删除确认 | 可关闭但不能绕过必要确认 |
| 空状态 | 无记录、无帖子、无资源 | 给出下一步动作 |
| 错误状态 | 网络、权限、会话、冲突 | 可读、可重试、无内部错误 |

### 6.2 今日组件

- 每日短句使用正文 18px 或 20px，出处使用 12px 或 14px。
- 心情选项排列留出足够热区，不使用连续打卡火焰或奖励。
- 快捷入口以文本和轻分隔组织，不做四格资讯卡片墙。

### 6.3 自测组件

- 题目正文最小 16px，长题目允许多行。
- 选项整行可点击，热区至少 44px。
- 进度显示“第几题 / 共几题”，不显示风险颜色。
- 结果页先显示解释，再显示行动入口；分数不是唯一视觉焦点。

### 6.4 安全支持组件

- 安全支持区使用浅安全砖红底和安全砖红小范围强调。
- 现实支持按钮放在安全支持说明之后、离开页面之前。
- 不出现模型图标、聊天气泡式 AI 解释或身份信息。

### 6.5 树洞组件

- 列表正文使用截断摘要，详情页才展开允许展示的正文。
- 匿名身份只显示匿名展示名，不显示可推断真实身份的字段。
- 检查中、等待确认、未公开、安全优先和已删除必须有不同文字状态。

## 7. 后台组件规范

后台使用项目内 Vue 组件，不使用 shadcn/ui 或大型组件库。

### 7.1 页面骨架

- 左侧为固定宽度的后台导航区域；
- 中央为任务列表或审计列表；
- W-03 详情页使用“必要事实与脱敏内容 / 当前操作”两栏；
- 右侧操作区域不得因为窗口缩放而覆盖事实区域；
- W-04 状态变体以页面内事实条和只读层表达，不用全屏错误弹窗遮住上下文。

### 7.2 后台组件清单

| 组件 | 规定 |
| --- | --- |
| 后台外壳 | 显示产品名称、当前账号、会话状态和退出 |
| 任务区块 | 三个固定标题：需要我处理、等待他人、最近处理 |
| 任务卡 | 只显示最小摘要、状态、时间、认领信息 |
| 事实列表 | 标签和值两列，长文本纵向展开 |
| 脱敏正文块 | 明确“已脱敏”，不可复制隐藏字段 |
| 操作面板 | 只显示当前任务允许动作，提交前确认 |
| 状态条 | 并发冲突、状态已变化、无权限、会话过期、提交失败 |
| 审计表格 | 只读、可筛选、可分页，不提供编辑和删除 |
| 重置确认 | 明示演示环境、数据范围和不可逆影响 |
| 桌面提示 | 低于 1280px 时阻断后台工作区 |

### 7.3 后台颜色规则

- 当前选中导航使用浅青表面和深静谧青文字。
- 任务状态使用文字和小色块，不做整行高饱和染色。
- 安全支持任务可以用浅安全砖红标出“需要优先查看”，但不标注自动风险等级。
- 已完成、等待他人和未配置要有文字标签，不能只靠颜色。

## 8. 响应式规则

### 8.1 学生端断点

学生端不是通用响应式网页，按微信小程序可用宽度设计：

| 宽度 | 规则 |
| --- | --- |
| 320px 至 359px | 维持单列布局，左右边距降为 16px，长文允许换行 |
| 360px 至 392px | 标准手机布局，左右边距 20px |
| 393px 至 430px | 标准手机布局，左右边距 24px |
| 大于 430px | 内容列保持可读最大宽度，不能无限拉长按钮和正文 |

### 8.2 后台断点

| 宽度 | 规则 |
| --- | --- |
| 小于 1280px | 只显示电脑浏览器提示，不显示可操作后台 |
| 1280px 至 1439px | 工作区可用，详情两栏按最小比例布局，允许纵向滚动 |
| 1440px 至 1919px | 标准工作台布局，设计基准为 1440×900 |
| 1920px 及以上 | 增加外围留白，不无限放大正文和表格字号 |

后台不实现移动端底部导航，也不把 W-02 的三个区块压成横向卡片流。

## 9. 文案和状态规范

- 使用“记录”“观察”“结果”“支持”而不是“诊断”“确定有病”“危险等级”。
- 使用“暂时没有保存成功，请稍后重试”而不是“系统错误”。
- 使用“内容正在检查”而不是“审核失败”，除非用户确实收到最终未公开状态。
- 使用“当前任务已被处理，页面仅供查看”表达并发变化。
- 使用“当前环境尚未配置”表达缺失的资源、身份服务或 AI 配置。
- 安全支持文案先给路径和资源，再给流程解释。
- 工作人员内部理由不可直接映射为学生可见羞辱标签。

## 10. 可访问性验收

- 所有按钮、输入框、单选项和列表项支持键盘或微信可访问操作。
- 触控热区不小于 44px。
- 页面缩放或系统大字模式下，重要按钮仍可点击，正文不被截断。
- 错误信息与对应输入关联，并且有文字说明。
- 状态变化提供可读文本，不只更新颜色或图标。
- 后台表格有表头语义，任务状态和操作权限对读屏可理解。
- 不用自动播放声音、强制震动或闪烁来表达安全状态。

## 11. 前端验收清单

### 学生端

- 四项底部导航顺序正确且当前项明确；
- 360px 至 430px 宽度没有横向溢出；
- 首次使用、身份核验、答题、结果、安全支持、发布和删除流程均有成功与失败状态；
- 安全状态没有多余分数、历史、AI 和身份信息；
- 撤回社区同意后仍可浏览，发帖和回应按钮禁用；
- 检查中和非公开帖子不能出现在公开列表。

### 后台

- 1440×900 下 W-01 至 W-05 结构完整；
- 1280px 下仍可操作，低于 1280px 显示提示；
- W-02 三个任务区块固定存在；
- W-03 两栏结构固定存在；
- W-04 五类异常状态可以复现；
- W-05 审计只读，重置只作用于演示环境；
- 任何受限字段都不会因为前端隐藏而被接口返回。
