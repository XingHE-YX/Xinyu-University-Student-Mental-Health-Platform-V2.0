# 阶段 8：完成前验收记录

日期：2026-09-02

本记录对应 `IMPLEMENTATION_PLAN.md` 第 11 节，证据分为本地可复现检查、演示模式检查和需要真实外部凭据的发布检查。未配置真实环境时，不把占位模板或游客 AppID 结果当作线上验收。

## 11.1 自动验证

| 运行单元 | 检查                                                     | 结果                                                                                           |
| -------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| 后端     | `backend/.venv/bin/ruff check .`                         | 通过                                                                                           |
| 后端     | `backend/.venv/bin/ruff format --check .`                | 81 个文件已格式化                                                                              |
| 后端     | `backend/.venv/bin/mypy .`                               | 81 个源文件无类型错误                                                                          |
| 后端     | `backend/.venv/bin/pytest`                               | 166 项通过                                                                                     |
| 后台     | `npm run typecheck`                                      | 通过                                                                                           |
| 后台     | `npm run lint`                                           | 通过                                                                                           |
| 后台     | `npm run format:check`                                   | 通过                                                                                           |
| 后台     | `npm run test`                                           | 3 个文件、5 项通过                                                                             |
| 后台     | `npm run build`                                          | Vite 构建通过                                                                                  |
| 学生端   | `npm run typecheck`                                      | 通过                                                                                           |
| 页面注册 | `app.json` 页面及 `.json/.ts/.wxml` 路径                 | 20 页，缺失 0                                                                                  |
| 部署配置 | CloudBase、学生端 profile、后台 hosting 示例校验         | 示例配置全部通过                                                                               |
| 函数包   | Python 3.11 manylinux2014 包构建、重复哈希和秘密文件扫描 | 两次 SHA-256 均为 `2759ddf675cf970db494bc04653d34aa20a09bb80f857c07d934335a0ed80ef3`，扫描通过 |
| 健康检查 | 本地 Uvicorn + `verify_health_then_contract.py`          | `status=degraded`，使用 `--allow-degraded` 检查通过；未配置环境保持降级                        |

后台门禁使用当前安装的 Node `v26.8.1` / npm `11.19.0` 完成；仓库契约仍锁定 Node `22.21.0` / npm `10.9.4`，工具版本差异已记录在 `lessons.md`。后续清洁环境发布仍需使用锁定版本。

## 11.2 关键人工路径

微信开发者工具 `Stable v2.01.2510290` 已打开 `miniprogram/`，模拟器成功渲染今日页并显示“今日 / 自测 / 树洞 / 我的”四项底部导航。演示响应夹具覆盖首次使用、同意/撤回、身份成功、今日心情、自测三模块、PHQ-9 安全三路、GAD-7、睡眠三维结果、树洞状态、历史逐条删除和账户停止/恢复；对应页面路径与 APP_FLOW.md 一致。

后台页面的 W-02 三任务区块、W-03 两栏详情、W-04 五类异常状态和 W-05 审计/演示重置均已由 Vitest、组件状态分支和静态结构检查覆盖。1440×900 的桌面布局、低于 1280px 的阻断组件、路由刷新回退和授权模式不显示演示审计均已由实现与构建检查确认。

真实微信预览、真机、CloudBase 业务 API、学校身份核验和静态托管入口仍未执行：工作区没有真实 AppID、EnvID、域名或授权凭据。游客 AppID 的 CLI 预览返回 `INVALID_LOGIN, access_token expired`，不能作为业务路径证据。

## 11.3 隐私和安全检查

- `admin/dist` 和学生端产物未发现 DeepSeek API Key、学校密钥、后台密码哈希、CloudBase 密钥或私钥标识。
- 学生端请求层只调用 Python 业务 API；未发现 CloudBase 管理接口或 DeepSeek 直连。
- 自测答案、姓名、学号和安全确认只作为受限业务请求输入；AI 服务使用服务端脱敏策略，审计保存最小事实。
- 后端模型和服务不接受客户端提供的 `score`、`role`、`safety_state`、`visibility_state` 或 `environment_id` 作为事实来源；评分和安全状态由服务端固定规则产生。
- 会话失效分支清除学生端会话令牌；后台会话失效分支清除详情并回到登录。

## 11.4 设计验收

- 学生端 20 个注册页面和四项底部导航与 APP_FLOW.md 一致，关键页使用统一页面容器、顶部栏、按钮、状态和确认弹层。
- 颜色、字号、间距、44/48px 热区、8px 以内圆角和 359/393px 断点遵循 FRONTEND_GUIDELINES.md；未发现渐变、发光、玻璃拟态、卡片墙、雷达图、心电图或红黄绿风险色阶。
- 后台保持桌面布局，未实现移动端底部导航；`DesktopWidthNotice` 在小于 1280px 时阻断可操作工作区，1440×900 使用两栏任务/审计结构。
- 安全确认和支持资源页不显示多余分数、历史、AI 或真实身份；睡眠结果只显示入睡、夜间情况和白天感受三个观察维度。

## 验收结论

本地代码、构建、静态隐私扫描和演示路径检查通过，可以提交阶段 8 的代码与验收记录。真实环境创建、微信真机/上传、CloudBase 函数和数据库部署、学校核验联调及静态托管发布属于凭据到位后的发布验收，不在当前工作区虚构完成。
